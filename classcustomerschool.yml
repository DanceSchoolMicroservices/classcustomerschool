server:
  port: 8083
  
spring:
  cloud:
    stream:
      kafka:
        bindings:
          ClassCustomerEvent:
            producer:
              sync: true

eureka:
  instance:
    preferIpAddress: true	
  client:
    hostname: localhost
    registerWithEureka: true
    fetchRegistry: true
