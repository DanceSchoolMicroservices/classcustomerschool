server:
  port: 8083
  
spring:
  cloud:
    stream:
      kafka:
        bindings:
          ClassCustomerEvent:
            producer:
              sync: true

eureka:
  instance:
    preferIpAddress: true	
  client:
    registerWithEureka: true
    fetchRegistry: true
